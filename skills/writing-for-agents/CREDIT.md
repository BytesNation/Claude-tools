# Credit

`SKILL.md` and `SKILL-MECHANICS.md` in this folder are the work of Matt Pocock,
taken from [mattpocock/skills](https://github.com/mattpocock/skills) under the MIT
licence. The full licence text sits beside them in `LICENSE`, and it is the licence
that governs those two files.

Two local changes:

- `AUDIT.md` is ours. Upstream is reference with no procedure attached, so this adds
  the editing pass that applies it to a target document, ending on the exhaustiveness
  bar the reference asks for but never states for itself.
- One line inserted into `SKILL.md`, beside the existing pointer to
  `SKILL-MECHANICS.md`, reaching `AUDIT.md`.

Everything else is upstream, unmodified. To refresh, re-fetch both files and
re-insert that one line.
