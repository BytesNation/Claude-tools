# Credit

`SKILL.md` in this folder is the work of Lauren Tan, taken from
[cursor/plugins](https://github.com/cursor/plugins/tree/main/pstack/skills/unslop)
under the MIT licence. The full licence text sits beside it in `LICENSE`, and it is
the licence that governs that file.

Two local changes, both one line:

- The `description` is scoped to prose a person reads and points at
  `writing-for-agents` for documents an agent consumes. Upstream says "any writing",
  which in this repo would have `unslop` adding voice to `SKILL.md` files.
- Step 1 of the process asks for all thirty-one patterns in turn rather than a scan,
  so the flat rule list carries an exhaustiveness bar.

Everything else is upstream, unmodified. To refresh, re-fetch `SKILL.md` and
re-apply those two lines.
